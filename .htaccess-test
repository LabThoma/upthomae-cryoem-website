# Minimal test .htaccess

RewriteEngine On

# Simple test to see if rewrite works at all

RewriteRule ^test$ php/simple-test.php [L]

# Handle API routes

RewriteRule ^api/(.\*)$ php/api-debug.php?path=/api/$1 [L,QSA]
